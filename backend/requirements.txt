fastapi
uvicorn
biopython
pydantic
primer3-py
pywebview>=4.0.0
requests>=2.25.0
# Hairpin Tm uses strider.thermo.hairpin.hairpin_thermo (bulge-aware, added in
# 0.3.2). Tan-Chen TBI salt correction for stems ≥6 bp is available upstream.
# Dimer Tm and suboptimal dimer enumeration are available upstream from v0.7.0.
# The v0.7.0 ThermoEngine.subopt() salt bug is fixed in upstream v1.1.0.
# strider.duplex_tm (primer3-style duplex Tm with Na/Mg/dNTP/oligo-conc params)
# was added upstream in v1.2.0.
# native-tm branch: Rust/PyO3 accelerator (strider._native) for melting_temperature,
# duplex_tm, duplex_dg, duplex_dh_ds + salt.py scalars. 15.4× vs pure Python.
# PR pending: https://github.com/EmilioVenegas/strider/pull/11
strider-dna @ git+https://github.com/KowalskiBio/strider.git@native-tm
