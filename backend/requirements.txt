fastapi
uvicorn
biopython
pydantic
primer3-py
pywebview>=4.0.0
requests>=2.25.0
# Stacked feature branch on the KowalskiBio fork, containing in order:
#   1. native-tm  — Rust/PyO3 accelerator (strider._native) for Tm/salt kernels   [PR #11]
#   2. native-dp  — native Rust port of the dimer MFE candidate DP                [PR #12]
#   3. mathews2004-dangles — corrected Mathews 2004 paramset (stack/bulge/interior
#      key conventions verified vs ViennaRNA eval), paramset threading on
#      hairpin/dimer APIs, name resolution, dangles=2 exterior stacks           [PR #13]
# main.py uses all three: dimer ΔG no-init reporting needs DUPLEX_INIT_ from the
# dimer module; parameter_set threading needs the corrected paramset + plumbing;
# hairpins need dangles=2 for IDT-consistent reports.  NOTE: pip will NOT
# reinstall a git-VCS dep whose version is unchanged — update_vm.sh force-
# reinstalls this package exactly because of that.
strider-dna @ git+https://github.com/KowalskiBio/strider.git@mathews2004-dangles
