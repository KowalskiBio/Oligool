fastapi
uvicorn
biopython
pydantic
primer3-py
pywebview>=4.0.0
requests>=2.25.0
# Hairpin Tm uses strider.thermo.hairpin.hairpin_thermo (bulge-aware, added in
# 0.3.2 via the merged loop-ΔH tables). 0.3.3+ adds Tan-Chen TBI salt correction
# for stems ≥6 bp and lazy-loaded torch in 0.3.4. Pinned for reproducible deploys.
strider-dna==0.3.4
